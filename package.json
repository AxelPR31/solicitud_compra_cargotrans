{
  "name": "solicitud-compra-cargotrans",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:stop": "node scripts/kill-dev-ports.mjs",
    "build": "pnpm -r build",
    "install:all": "pnpm install"
  },
  "engines": {
    "node": ">=20"
  }
}
